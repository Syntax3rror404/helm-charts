# helm install minecraft . -n minecraft-test -f example-neoforge.yaml --create-namespace
# helm upgrade minecraft . -n minecraft-test -f example-neoforge.yaml
# helm uninstall minecraft -n minecraft-test ; kubectl delete ns minecraft-test
---
service:
  port: 25565
  labels:
    lb.cilium.io/pool: general

persistence:
  enabled: true
  storageClass: "longhorn-staging"

neoforge:
  enabled: true
  version: "21.1.219"

minecraft:
  eulaAccepted: true
  serverProperties:
    levelName: "modded-world"
    whiteList: "false"
